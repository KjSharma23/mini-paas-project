apiVersion: apps/v1
kind: Deployment
metadata:
  name: mini-paas-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: mini-paas
  template:
    metadata:
      labels:
        app: mini-paas
    spec:
      containers:
      - name: mini-paas-container
        image: kajal2415/mini-paas:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 5000
